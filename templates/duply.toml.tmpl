[template]
src = "prometheus.yml.tmpl"
dest = "/etc/duply/prometheus/conf"
prefix = "/%%PROJECT%%/%%ENVIRONMENT%%/prometheus"

uid = 0
gid = 0
mode = "0640"

keys = [
    "/config/*",
    "/config"
]
